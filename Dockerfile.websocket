FROM node:18-alpine

WORKDIR /app

# 复制 package 文件
COPY package*.json ./

# 只安装生产依赖
RUN npm ci --only=production

# 复制 WebSocket 服务器代码
COPY server/websocket.js ./server/

# 设置环境变量
ENV PORT=8080
ENV NODE_ENV=production

# 暴露端口
EXPOSE 8080

# 启动 WebSocket 服务器
CMD ["node", "server/websocket.js"]

